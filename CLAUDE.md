# CLAUDE.md

## プロジェクト概要

九州産業大学の任意団体、九産模型愛好会のwebサイトを構築します。

## 技術スタック

言語: HTML,PHP,CSS  
サーバ導入済みソフト: MySQL,apache  
コーディング規約: PSR-2に準拠


## ドキュメント

`docs/`配下にこれから構築するものを定義する設計草案が置かれています。

- `docs/ksmc_youkenteigisho_draft.md` — 要件定義書（日本語、v0.4ドラフト）。機能要件（FR-xx）、非機能要件（NFR-xx）、ロール定義、未解決の検討事項（第11章）に関する一次情報源。
- `docs/db_schema_draft_1.md` — DBスキーマ草案（v0.1）。Mermaid製ER図、テーブル定義、および上記FR/NFR番号への明示的な対応関係を含む。
- `docs/sitemap.md` — 画面遷移を示すMermaidフローチャート。ロール（会員／管理者／広報担当／休止会員）別に整理されている。
- `docs/ksmcweb_frontSample.pdf` — フロントエンドの見た目のサンプル／モックアップ。
- `erdiagram.md` — 参照の必要なし。

機能を実装する際は要件定義書を一次情報源として扱い、新規コードを対応するFR/NFR番号にたどれるようにしてください。要件定義書第11.2節には「要決定」と明記された項目がいくつかあります（例：1投稿あたりのアップロード容量上限の具体的な数値、初期パスワードの配布方法、ロゴ合成フォームのUX仕様など）。これらについて仮定でハードコーディングする前に、必ずユーザーに確認してください。

## 注意事項

- **CMS・フレームワークは使用しない。** 
- **対象サーバーはリソース制約が厳しい**：RHEL 10.1、1 vCPU、メモリ1GB、ディスク50GB。常駐プロセス数・MySQL同時接続数を抑えた設計とすること。ディスク使用量は投稿単位のアップロード上限によって制御する（NFR-05）。
- **ロールは4種類（顧問用ロールは設けない）**：会員（デフォルト。自分の投稿の作成・編集が可能）／広報担当（会員の権限に加え、公式ブログへの投稿が可能）／管理者（全会員・全投稿へのCRUD、招待、ロール付与が可能）／休止会員（退会・卒業した元会員。ログイン・パスワード再設定・自分の既存投稿の編集削除は可能だが、新規投稿とプロフィール編集は不可）。
- **会員登録は招待制のみ**
- **投稿の状態は「下書き」「公開」の2状態のみ**
- **2種類の記事種別が同一の`posts`テーブルに同居する**（`post_type`：`individual`／`official_blog`）。`official_blog`を投稿できるのは広報担当・管理者ロールのみで、この制約はDBのCHECK制約ではなくアプリケーション層で担保する。
- **画像アップロードはロゴ合成に対応**
- **タグ付け**：同一フォーム内で自由入力による新規タグ登録と既存タグからの選択の両方を許可する。管理者は表記ゆれのタグを統合・リネームできる。
- **推奨文字コード**：UTF-8mb4。
- **バックアップ**：当面は手動・ローカル運用（初期実装では自動バックアップの仕組みは想定していない）。
- **MVCモデルを使用**
- **インラインスタイルの使用禁止**
- **機能項目の実装毎にブランチを作成**
- **不明点は必ず質問**
- **会話・文書には日本語を使用**
- **サーバのパスワード、IPアドレス、APIキーなどは.gitignoreや環境変数で処理**

